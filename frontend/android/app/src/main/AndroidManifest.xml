<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        allowBackup is false because the Supabase auth session lives in the
        WebView's local storage. With backups enabled, a logged-in session can
        be pulled off a device with `adb backup` or carried to another phone by
        device transfer. The rules files below keep the same guarantee on the
        Android versions that honour them.
    -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        tools:replace="android:usesCleartextTraffic">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Auth deep links. Only these two exact hosts are handled; the app
                accepts nothing from them except a one-time PKCE code (see
                DeepLinkHandler in src/App.jsx and AUTH_DEEP_LINKS.md).
                  spendly://login-callback   Google sign-in, email confirmation
                  spendly://reset-password   password-reset email
                Custom schemes are not verified by Android; PKCE is what keeps a
                hijacked link from yielding a session. Android App Links (https)
                are the recommended upgrade once a domain is verified.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="spendly" android:host="login-callback" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="spendly" android:host="reset-password" />
            </intent-filter>

        </activity>

        <!-- UPI Payment Notification Listener Service -->
        <service
            android:name=".PaymentNotificationListener"
            android:label="Vittova Payment Tracker"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!--
        Permissions. Deliberately minimal.
        - INTERNET: talk to the Vittova API and Supabase.
        Notification access is NOT a manifest permission: the listener service
        above is protected by BIND_NOTIFICATION_LISTENER_SERVICE (which only the
        system holds), and the user grants access in Android settings.
        POST_NOTIFICATIONS was removed: Vittova does not post notifications.
        SMS permissions are not requested.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
